"""Session 10 — retrieval relevance work.

Starting point: only the cross-encoder wrapper handed out with the exercise
(``reranker``) plus its pre-flight check (``verify_reranker``). Hybrid search,
rank fusion and the recall-then-rerank composition are what the exercise asks
you to build here — how you split them into modules is your call.
"""
